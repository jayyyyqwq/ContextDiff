# ContextDiff
ContextDiff Traditional platforms treat every developer like a beginner. If you know Java and want to learn Rust, you don't need a tutorial on loops—you need the conceptual delta. ContextDiff is a compiler for human knowledge. Instead of teaching from zero, we teach from context.
